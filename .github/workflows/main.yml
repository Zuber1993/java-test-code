name: test maven
on: push
jobs:
  clone:
    runs-on: ubuntu-latest
    steps:
      - name: download code
        uses: actions/checkout@v3
      - name: Setup Java
        uses: actions/setup-java@3
        with:
           java-version: 11

      - name: Build project
        run: mvn install